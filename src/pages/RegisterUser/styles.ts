import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    margin: 10,
  },

  buttonMargin: {
    marginTop: 10
  }
});

export default styles;